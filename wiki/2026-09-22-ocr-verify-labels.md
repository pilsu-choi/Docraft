---
type: Report
title: "AO 교차검증 정답셋(라벨)과 단계별 정확도 평가 스크립트"
description: "VLM으로 문서 유형 4종의 정답셋을 라벨링하고 36건을 이미지 대조로 검수한 뒤, 표 행 식별자 정렬과 rules.same 일원화로 보정한 raw/rules/ao/final 4단계 정확도 측정 기록"
tags: [ocr-verify, agentic-ocr, evaluation, labeling, vlm, harness-v2, label-review]
status: active
---

# AO 교차검증 정답셋(라벨)과 단계별 정확도 평가 스크립트

2026-09-22 · 브랜치 `feat/ocr-verify` · 워크트리 `.worktrees/ocr-verify`

[Agentic OCR 2.0 결과 교차검증·자동 교정 API 계획](2026-09-22-ocr-verify-plan.md)의 정답셋 구축·측정 파트를 맡아
`scripts/verify_label.py`(라벨 생성)와 `scripts/verify_eval.py`(단계별 정확도 계산)를 구현했다. 같은 워크트리에서
다른 에이전트가 `backend/doctypes.py`·`backend/rules.py`·`backend/main.py`·`backend/verify.py`·`tests/`를 동시에
구현했고, 작업 도중 세 모듈 모두 완성되어 두 스크립트 모두 실제 파이프라인 전 단계를 검증할 수 있었다.

## 1. 정답셋(`scripts/verify_label.py`)

- 문서 유형 4종(진단서·소견서·진료비영수증·세부내역서)마다 gold 1건(AO 예시 이미지) + silver 8건
  (`data/files/<유형>_samples/`에서 파일명 정렬 후 균등 간격 추출, tif 최소 3장 보장)을 골라 이미지만 보여주고
  (OCR 텍스트 없이) VLM이 필드값을 읽게 했다.
- 라벨링 모델은 추출 모델(`qwen/qwen3-vl-32b-instruct`)과 겹치면 순환 평가가 되므로, OpenRouter `/models`를
  조회해 후보(`anthropic/claude-sonnet-4.5` → `google/gemini-2.5-pro` → `openai/gpt-5`) 중 첫 available를 골랐다
  — 실제로는 `anthropic/claude-sonnet-4.5`가 사용됐다. `backend/engine.py`는 건드리지 않고, `httpx.Client.post`를
  얇게 감싸 요청 body의 `model` 키만 라벨링 모델로 바꿔치기했다(`ai_settings()`는 그대로 두어 다른 코드 경로에
  영향이 없다).
- 스키마는 `backend.doctypes.schema(doc_type)`(작업 도중 다른 에이전트가 구현 완료)를 우선 쓰고, 실패하면
  AO 예시 json의 키만으로 자체 생성하는 `fallback_schema()`를 쓴다 — 이 fallback은 `scripts/verify_eval.py`의
  `schema_for()`에서도 그대로 재사용해 중복 스키마 정의를 두지 않았다.
- 결과: **gold 4건 + silver 32건 = 36개 라벨**을 전부 생성(`data/verify/labels/<유형>/<이미지 stem>.json`),
  ok=35 · skip=1(먼저 만든 gold 진단서 1건) · error=0, 총 176.4초.

## 2. 단계별 정확도(`scripts/verify_eval.py`)

- 4단계: `raw`(`engine.extract`) → `rules`(`rules.apply`) → `ao`(gold만, `verify.flatten`으로 AO 값 평탄화,
  `backend.verify`의 기존 함수를 그대로 재사용해 중복 구현을 만들지 않았다) → `final`(gold만, `verify.run(image,
  ao_json, doc_type)`의 최종값).
- 채점: 스칼라는 `rules.same(kind, 라벨, 예측)`, 표는 라벨 행·예측 행을 순서대로 짝짓고 셀 단위로 같은 비교를
  적용한다(행 수가 다르면 모자란 쪽을 빈 행으로 채워 누락·과잉이 자연히 오답/오탐으로 집계됨). null 라벨은
  분모에서 빼고, 라벨이 null인데 예측이 있으면 별도로 오탐(`fp`) 카운트.
- `doctypes`/`rules`/`verify` 중 하나라도 아직 `NotImplementedError`/`ImportError`면 그 단계만 "미구현"으로
  표시하고 스크립트는 끝까지 돈다(방어적 스켈레톤 — 지금은 셋 다 구현되어 실제로는 발동하지 않았다).
- 캐시: 이미지당 파싱 20~90초가 걸려 `data/verify/cache/<유형>__<stem>.parse.json`·`.extract.json`에 저장하고
  `--no-cache`로 무시한다. `--workers`(기본 2)로 PaddleOCR 서버 과부하를 막는다.

### 실행 결과 (라벨 검수 전, 2026-09-22 15:37/15:52)

**gold 4건, 전 단계**:

| 유형 | raw | rules | ao | final |
|---|---|---|---|---|
| 진단서 | 70.6% (12/17) fp=1 | 76.5% (13/17) fp=3 | 52.9% (9/17) fp=10 | 70.6% (12/17) fp=10 |
| 소견서 | 44.4% (4/9) | 44.4% (4/9) fp=5 | 55.6% (5/9) fp=5 | 55.6% (5/9) fp=6 |
| 진료비영수증 | 59.3% (16/27) fp=2 | 59.3% (16/27) fp=4 | 48.1% (13/27) fp=170 | 59.3% (16/27) fp=171 |
| 세부내역서 | 89.0% (210/236) fp=16 | 89.0% (210/236) fp=17 | 83.1% (196/236) fp=16 | 89.0% (210/236) fp=53 |

**전체 36건(gold+silver), raw·rules**:

| 유형 | raw | rules |
|---|---|---|
| 진단서 | 79.6% (82/103) fp=21 | 80.6% (83/103) fp=70 |
| 소견서 | 60.3% (70/116) fp=23 | 60.3% (70/116) fp=54 |
| 진료비영수증 | 69.2% (522/754) fp=250 | 69.5% (524/754) fp=268 |
| 세부내역서 | 80.8% (1587/1965) fp=378 | 80.8% (1587/1965) fp=379 |

이 수치는 `final < ao`·`final < raw`처럼 앞뒤가 맞지 않았고, 진료비영수증 ao 단계에서 오탐이 170건이나
나왔다. 원인은 (1) VLM 라벨 자체의 오류, (2) 표를 **위치로만** 짝짓는 채점, (3) 라벨 null과 예측 `"0"`을
무조건 오탐으로 세는 집계 — 셋 다였다. 아래 3·4절이 그 교정 기록이다.

## 3. 라벨 36건 이미지 대조 검수

VLM(claude-sonnet-4.5)이 이미지만 보고 만든 라벨은 오류율이 높았다. 36건 전부를 이미지와 직접 대조해
고쳤다(진단서 gold 1건은 앞서 코디네이터가 교정해 두어 그 파일의 표기를 기준 관례로 삼았다).
유형별로 검수 에이전트를 병렬로 띄웠고, 표가 큰 유형(진료비영수증·세부내역서)은 이미지를 PIL로 2~5배
확대·분할 crop해 읽게 했다.

### 표기 관례 (AO 응답 관례를 따른다)

- 날짜 `YYYYMMDD`, 금액은 숫자만, 병명코드는 점 없이(`R63.4` → `R634`), 주민번호는 `601113-1472313`
  (마스킹은 `*` 유지). 문서에 없는 값은 null — 지어내지 않는다.
- 진단서·소견서: `병명`은 주/부상병 표시가 있으면 `(주상병) …`로 접두. `차트번호`는 발행번호·연번호·
  문서번호, `환자 등록번호`는 등록번호·병록번호. `성별`·`생년월일`은 칸이 없어도 주민번호에서 유도
  (뒷자리 1·3→남, 2·4→여 / 1·2→19xx, 3·4→20xx). `진단일` 칸이 비어 있으면 발급일로 대체하지 않고 null.
  `검사·치료·수술내역`은 전용 표가 없어도 비고·소견 문장에서 행을 만든다(날짜가 없으면 날짜 열 null).
  `통원일`은 문서의 통원 날짜 전부를 `", "`로 잇는다. `병원연락처`는 병원 전화만(환자 전화 아님).
- 진료비영수증: `외래/입원`은 입원 `01`·외래 `02`. 표는 합계 행을 빼고, **인쇄된 항목행은 금액이 비어도
  모두 포함**하고 순서를 유지하며 빈 셀은 null. `급여`·`비급여`가 하위 열을 묶는 머리글인 서식에서는
  그 열을 null로 둔다. 우측 합계 필드는 문서 인쇄값 그대로(직접 계산하지 않는다).
- 세부내역서: 표는 문서 순서, 합계·계·끝수처리 행 제외. `환자정보(입통원구분)`은 통원/입원.
- 검수한 파일에는 `reviewed_by`를, 읽을 수 없던 값이 있으면 `notes`를 남겼다.

### 파일별 수정량

| 유형 | 파일 | 고친 스칼라 | 표 행 (전 → 후) | 비고 |
|---|---|---|---|---|
| 진단서 | 20230228163252727030 (gold) | — | — | 사전 교정본(기준 관례) |
| 진단서 | 7 | 4 | 수술내역 0 → 1 | 의사명이 병원 스탬프 오인식 |
| 진단서 | [0010]20201005133037097100002F000 | 4 | 수술내역 0 → 1 | 병원명·도로명 오독 |
| 진단서 | [0010]202010051520270844006 | 5 | 수술 0→1, 치료 0→1 | 입원일수를 통원일수로 오기 |
| 진단서 | [0010]202010061559360779008 | 6 | 치료내역 0 → 1 | 발병일을 입원일자로 오기, 병명코드 점 제거 |
| 진단서 | [0010]20201007100925021700005F000 | 3 | 수술·검사·치료 0 → 각 1 | 병원주소 일부 도장에 가림 |
| 진단서 | [0010]202010141007520740005 | 2 | 수술내역 0 → 1 | 병명 오타(급소→국소) |
| 진단서 | [0010]202010151114530362004 | 8 | 수술·검사·치료 0 → 각 1 | 병원명 전혀 다름, 입·퇴원일 누락 |
| 진단서 | [0020]202010051051093670^…10090002 | 4 | 수술·검사 0→1, 치료 0→2 | 병원명 자간 분리 |
| 소견서 | 202010061144570036033 (gold) | 4 | 치료내역 0 → 1 | 병원명·주소·병명 오독 |
| 소견서 | 110616490642220201005_150823103_263318420_2 | 7 | 검사·치료 0 → 각 1 | 병원명·주소 전면 오독, 환자정보 마스킹 |
| 소견서 | 16.소견서 7 | 6 | 치료내역 0 → 1 | 생년월일·통원일을 주민번호/내원일에서 보완 |
| 소견서 | 16.소견서 10 | 15 | 검사내역 검사명 3건 보완, 치료 0 → 1 | 오류 최다(병원명·면허번호·주민번호·병명코드) |
| 소견서 | 신한life_test_220316_소견서15 | 11 | 치료내역 0 → 1 | 병원명·병원주소·병원연락처에 환자 정보가 들어가 있었음 |
| 소견서 | [0010]202010051233570142016 | 4 | 수술내역 0 → 1 | 병명코드 마지막 글자 오독 |
| 소견서 | [0010]20201005125758029000009R000 | 3 | 치료내역 0 → 1 | 병명 오독(제기뼈→쐐기뼈) |
| 소견서 | [0010]202010141134480807013 | 4 | — | 입·퇴원일이 본문에만 있어 누락 |
| 소견서 | [0020]202010051447385900^…10380005 | 9 | 수술 0→1, 치료 0→3 | 병명·병명코드 다수 오독 |
| 진료비영수증 | SA2019112214096_2019112215445100 (gold) | 10 | 2 → 26 | 진료비총액·기관명·사업자번호 오독 |
| 진료비영수증 | SA2019101694203_IT21_1 | 7 | 24 → 25 | 열 매핑이 한 칸씩 밀려 있었음 |
| 진료비영수증 | SA2019110187887_IT23_5 | 9 | 28 → 29 | 의료기관 정보가 통째로 다른 병원 |
| 진료비영수증 | 1546387085517921 | 6 | 2 → 24 | 기관명에 주소가 들어가 있었음 |
| 진료비영수증 | 1557962688368787 | 8 | 9 → 15 | 카드 결제를 현금으로 기입 |
| 진료비영수증 | 5Sb.(1)진료비영수증_CFSA012600H21162002019050709051 8537_1 | 9 | 11 → 25 | 하위행 누락으로 금액이 한 칸씩 밀림 |
| 진료비영수증 | SA2019070400018_2019070411283501 | 9 | 3 → 25 | 영수증번호를 환자등록번호로 기입 |
| 진료비영수증 | SA2019101165425_2019101517571103 | 14 | 18 → 28 | 우측 합계를 직접 계산한 값으로 채움 |
| 진료비영수증 | SA2019101165425_2019101517571107 | 13 | 3 → 28 | 금액 귀속 오류(검사료→외래관리료 등) |
| 세부내역서 | 2303314528 (gold) | 1 | 16 → 16 (셀 약 50) | EDI명칭 16개 중 15개가 환각 |
| 세부내역서 | 16406102833801004940650714586571 | 1 | 13 → 13 (셀 약 8) | 2/2쪽이라 급여총액 없음 |
| 세부내역서 | 1120210831113318992_…69243301539141842 | 8 | 30 → 30 (셀 약 75) | 1/9쪽, 소계를 총액으로 오기 |
| 세부내역서 | 2303314666 | 5 | 6 → 6 (셀 약 14) | 사진 기울어져 금액이 한 행씩 밀림 |
| 세부내역서 | 2303315731 | 8 | 37 → 37 (셀 약 300) | 오류 최다, 면허번호를 본인부담총액으로 기입 |
| 세부내역서 | 3022030314525206-1 | 5 | 1 → 1 (셀 1) | 합계행이 한 칸씩 밀림 |
| 세부내역서 | SA2020010682876_3020010608495802 | 1 | 2 → 2 (셀 3) | EDI명칭 환각(우로→무료) |
| 세부내역서 | SA2020010683612_2020010615532105 | 1 | 25 → 25 (셀 약 150) | 자보인데 총액을 본인부담에 넣음 |
| 세부내역서 | photo_1640582155063 | 0 | 23 → 23 (셀 약 110) | 단가 열 누락으로 열이 한 칸씩 밀림 |

### 라벨 오류의 반복 패턴

1. **표 행 대량 누락** — 금액이 0·공란인 행을 통째로 버려 진료비영수증은 3~18행만 남았다(실제 24~29행).
   행이 빠지면 아래 행 금액이 위 행 이름에 붙어 **행 오귀속**이 연쇄로 생긴다.
2. **EDI명칭 환각** — 세부내역서 명칭 열은 읽지 못한 글자를 그럴듯한 약품명으로 지어냈다(한 파일에서 16개
   중 15개가 환각).
3. **열 밀림** — 단가 열을 빠뜨리거나 합계행을 한 칸씩 밀려 읽어 수치 전체가 어긋났다.
4. **머리글 문자열 유입** — `환자정보-질병군(DRG)번호`·`환자정보-환자구분`에 표 머리글이 그대로 들어갔다.
5. **직접 계산한 합계** — 우측 합계를 인쇄값 대신 계산값으로 채웠다(끝수처리 때문에 불일치).
6. **다른 필드 값 유입** — 환자 전화번호를 병원연락처에, 영수증번호를 환자등록번호에, 면허번호를
   본인부담총액에 넣었다.

## 4. 평가 스크립트 보정(`scripts/verify_eval.py`)

1. **표 행 정렬** — `ROW_KEYS`(진료비영수증·세부내역서 `항목`+`EDI코드`, 진단서류 `병명코드`·`수술일자`·
   `검사일`·`치료일`·`행위일`)로 라벨 행과 예측 행을 **먼저 식별자로 짝짓고**, 남은 행만 순서대로 짝짓는다
   (`pair_rows()`). 끝내 짝이 없는 행은 빈 행과 맞물려 누락·과잉으로 잡힌다. 행 하나만 빠져도 그 아래가
   전부 오답이 되던 문제가 사라졌다.
2. **오탐 집계 일원화** — `verdict()`가 `rules.same` 하나로 correct/wrong/fp/skip을 가른다. 라벨 null과
   예측 `"0"`·빈 문자열을 오탐으로 볼지는 이제 `rules.same`이 정한다(별도 비교 함수를 두지 않았다).
3. **final 캐시 없음** — 캐시는 raw·rules가 쓰는 parse·extract뿐이다. `final`은 매번 `verify.run`을 새로
   호출하므로 `backend/verify.py`가 바뀌면 곧바로 반영된다(docstring에 명시).
4. **verbose 출력** — 오답 줄마다 `단계 | 이미지 | 필드: 라벨 | 예측`을 찍고, 유형·필드별 실패를 합산한
   `실패 상위 20 필드` 표를 항상 출력한다(룰 확장 대상을 바로 고를 수 있게).

## 5. 재평가 결과 (검수 후, 2026-09-22 16:21 · 36건 전 단계 205.5초)

| 유형 | raw | rules | ao | final |
|---|---|---|---|---|
| 진단서 | 82.0% (109/133) fp=6 | 83.5% (111/133) fp=55 | 100.0% (25/25) fp=1 | 100.0% (25/25) fp=2 |
| 소견서 | 82.8% (120/145) fp=8 | 82.8% (120/145) fp=39 | 90.0% (9/10) | 90.0% (9/10) fp=1 |
| 진료비영수증 | 85.6% (889/1039) fp=47 | 85.7% (890/1039) fp=71 | 98.0% (49/50) fp=12 | 100.0% (50/50) fp=11 |
| 세부내역서 | 94.5% (1844/1951) fp=297 | 94.5% (1844/1951) fp=298 | 92.7% (204/220) fp=19 | 98.6% (217/220) fp=35 |

검수 전과 비교하면 raw는 진단서 79.6→82.0%, 소견서 60.3→82.8%, 진료비영수증 69.2→85.6%,
세부내역서 80.8→94.5%로 올랐다. gold 4건의 `ao`는 52.9/55.6/48.1/83.1% → 100/90/98/92.7%,
`final`은 70.6/55.6/59.3/89.0% → 100/90/100/98.6%가 됐고, **진료비영수증 ao 오탐 170건 → 12건**으로
줄었다. 이제 모든 유형에서 `final ≥ ao`이고 진단서·진료비영수증은 `final`이 만점이다 — 단계가 올라갈수록
값이 좋아진다는 파이프라인의 기본 가정이 비로소 수치로 확인됐다. (이 개선은 라벨 검수, 행 식별자 정렬,
그리고 같은 시기에 다른 에이전트가 손본 `rules.same`의 표기차 허용이 함께 작용한 결과다.)

### 실패 상위 20 필드 (룰 확장 대상)

셀은 `오답수/분모`, `fp`는 오탐.

| # | 유형 | 필드 | 실패 | raw | rules | ao | final |
|---|---|---|---|---|---|---|---|
| 1 | 세부내역서 | 항목내역.원내코드 | 260 | 0/1 fp=122 | 0/1 fp=122 | - | 0/0 fp=16 |
| 2 | 세부내역서 | 항목내역.급여 | 245 | 0/0 fp=120 | 0/0 fp=120 | 0/0 fp=3 | 0/0 fp=2 |
| 3 | 진료비영수증 | 항목내역.항목 | 220 | 109/222 | 109/222 | 0/26 fp=1 | 0/26 fp=1 |
| 4 | 세부내역서 | 항목내역.EDI코드 | 142 | 71/149 | 71/149 | - | - |
| 5 | 세부내역서 | 항목내역.단가 | 70 | 0/114 fp=35 | 0/114 fp=35 | - | - |
| 6 | 진료비영수증 | 항목내역.급여 | 61 | 0/0 fp=29 | 0/0 fp=29 | 0/0 fp=3 | - |
| 7 | 세부내역서 | 항목내역.EDI명칭 | 37 | 17/152 | 17/152 | 1/16 | 2/16 |
| 8 | 세부내역서 | 항목내역.종료일자 | 32 | - | - | 0/0 fp=16 | 0/0 fp=16 |
| 9 | 진료비영수증 | 항목내역.공단부담금 | 24 | 10/136 fp=1 | 10/136 fp=1 | 0/2 fp=1 | 0/2 fp=1 |
| 10 | 진료비영수증 | 항목내역.본인부담금 | 24 | 10/136 fp=1 | 10/136 fp=1 | 0/2 fp=1 | 0/2 fp=1 |
| 11 | 세부내역서 | 급여_급여총액 | 20 | 3/3 fp=6 | 3/3 fp=6 | 1/1 | 1/1 |
| 12 | 소견서 | 치료내역.치료명 | 18 | 9/9 | 9/9 | - | - |
| 13 | 진료비영수증 | 항목내역.비급여 | 18 | 0/0 fp=7 | 0/0 fp=7 | 0/0 fp=2 | 0/0 fp=2 |
| 14 | 세부내역서 | 항목내역.항목 | 16 | 1/152 | 1/152 | 14/16 | - |
| 15 | 진단서 | 치료내역.치료명 | 14 | 7/7 | 7/7 | - | - |
| 16 | 진료비영수증 | 환자정보-질병군(DRG)번호 | 12 | 0/0 fp=2 | 0/0 fp=9 | - | 0/0 fp=1 |
| 17 | 세부내역서 | 항목내역.급여구분 | 10 | 3/150 fp=2 | 3/150 fp=2 | - | - |
| 18 | 소견서 | 이름 | 9 | 2/3 | 2/3 fp=5 | - | - |
| 19 | 진단서 | 성별 | 9 | 1/1 | 0/1 fp=8 | - | - |
| 20 | 진료비영수증 | 항목내역.선택진료료외 | 9 | 4/124 | 4/124 | - | 0/1 fp=1 |

읽는 법: 남은 실패는 크게 세 갈래다.

- **null 정책 불일치** (1·2·6·8·11·13·16번) — 라벨은 문서에 칸이 없으면 null인데 추출은 `0`·빈 문자열을
  채운다. `backend/doctypes.py`의 `RECEIPT_HINT`는 아직 "값이 없는 열은 0"이라 라벨(빈 셀 null)과
  어긋난다. 힌트를 null로 바꾸거나 `rules.same`에서 `0 ≡ null`을 더 넓게 허용해야 한다.
- **자유 텍스트 열** (3·7·12·14·15번) — 항목명·EDI명칭·치료명. 항목명은 `투약및조제료_행위료` 같은
  표기 통일 규칙을 `rules`로 옮기면 대부분 잡히고, EDI명칭은 셀 폭에서 잘린 원문을 어떻게 볼지
  (부분 일치 허용) 정해야 한다.
- **자릿수·표기** (4·5·9·10·17·18·19·20번) — EDI코드의 `O/0`·`I/1` 혼동, 단가의 소수점, 성별을 주민번호
  에서 유도하지 않는 문제. `rules`의 필드별 정제를 늘릴 자리다.

## 6. 사용법

```bash
# 라벨 생성 (gold 4 + silver 32, 재실행 시 이미 있는 라벨은 건너뜀)
../../.venv/bin/python scripts/verify_label.py
../../.venv/bin/python scripts/verify_label.py --doc-type 진단서 --per-type 4 --force
../../.venv/bin/python scripts/verify_label.py --only-gold --model anthropic/claude-sonnet-4.5

# 단계별 정확도
../../.venv/bin/python scripts/verify_eval.py --grade gold --stage raw   # 동작 확인용, 빠름
../../.venv/bin/python scripts/verify_eval.py --verbose                  # 전체, 오답 목록까지
../../.venv/bin/python scripts/verify_eval.py --doc-type 진단서 --no-cache --workers 3
```

## 7. 한계·후속 과제

- 36건 전부 이미지 대조를 거쳤지만 **팩스·사진 품질 때문에 확신이 낮은 값이 남아 있다**. 해당 파일에는
  `notes`를 달아 두었다. 특히 의심스러운 것:
  - `진료비영수증/1546387085517921`·`1557962688368787`·`5Sb.(1)…`·`SA2019101694203_IT21_1`·
    `SA2019110187887_IT23_5` — `공단부담총액` 칸이 서식에 없다고 보고 null로 두었다. 합계행 ②를 쓸지
    말지는 AO·추출 쪽 관례와 맞춰야 한다(라벨과 추출이 서로 다르면 그대로 오답으로 잡힌다).
  - `세부내역서/1120210831113318992…`·`2303315731`·`16406102833801004940650714586571`·
    `3022030314525206-1`·`SA2020010683612…` — 여러 쪽짜리 문서의 한 쪽만 이미지로 있어 총합계 행이 없다.
    합계 필드를 전부 null로 두었는데, 소계를 총액으로 볼지 기준이 필요하다.
  - `진료비영수증/5Sb.(1)…`의 환자 성명(박결규/박경규), `세부내역서/2303315731`의 일부 EDI명칭,
    `세부내역서/16406102833801004940650714586571`의 치료재료 코드 `B102010Z`(1/I 구분 불가).
  - `진료비영수증/SA2019101165425_2019101517571103`·`…107`의 26번째 행은 항목명이 인쇄되지 않아 null.
- `backend/doctypes.py`의 `RECEIPT_HINT`는 "값이 없는 열은 0", 라벨 관례는 "빈 셀은 null"이라 서로 다르다.
  둘 중 하나로 맞추거나 `rules.same`이 `0 ≡ null`을 흡수해야 진료비영수증 표의 오탐이 더 줄어든다.
- `ao`/`final` 단계는 gold 4건뿐이라 표본이 작다 — silver에도 AO 응답이 있다면(harness-v2 쪽에 요청 필요)
  표본을 늘릴 수 있다.

홀드아웃 40건의 이미지 대조 검수는 [label-review](2026-09-23-label-review.md)에 기록했다.
