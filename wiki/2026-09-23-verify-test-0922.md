---
okf_version: "0.2"
type: Test Report
title: "0922 테스트 산출물 59건 AWS 교차검증 재테스트"
description: "harness-v2 [진료비영수증]이슈_정리_260923/0922_테스트_산출물.zip의 진료비영수증 30건·세부내역서 29건을 AWS에 배포한 /api/verify로 돌리고, AO 대비 바뀐 값을 원본 이미지와 대조해 교정이 맞는지 감사한 결과. 영수증은 대체로 개선, 세부내역서는 집계 행 삭제로 악화. 테스트 중 찾은 /api/verify 이벤트 루프 차단 버그 수정 포함"
tags: [verify, test, aws, 진료비영수증, 세부내역서, harness]
generated: {by: claude-code/claude-opus-5-5, at: 2026-09-23}
status: active
---

# 0922 테스트 산출물 59건 AWS 교차검증 재테스트

- 날짜: 2026-09-23
- 브랜치: `fix/verify-nonblocking`(코드 수정), `docs/verify-test-0922`(이 기록)
- 워크트리: `.worktrees/verify-nonblocking`, `.worktrees/verify-test-0922`

## 방법

- 자료는 `harness-v2/docs/requirements/[진료비영수증]이슈_정리_260923/0922_테스트_산출물.zip`이다. 문서마다 원본, `preprocessed.png`, AO 응답 `ocr.json`, 하네스 결과 `harness.json`(필드별 `tier`·규칙 pass/fail)이 들어 있다. zip 안 파일 이름은 cp437로 깨져 있어 복원해 풀었다.
- AWS 서버(`8a0920d` 배포)의 `/api/verify`에 `preprocessed.png` + `ocr.json`을 보냈다. 힌트 없이 전체 필드를 판정했고, 동시 4건이며, 추출·Judge는 `qwen/qwen3-vl-32b-instruct`다. 59건 모두 성공했고 평균 129초가 걸렸다.
  - AO가 문서 유형을 코드값(`Y000701200`)으로 내고 필드를 하나도 추출하지 않은 1건은 `doc_type=진료비영수증`을 지정해 다시 보냈다.
- AO 대비 바뀐 값(표 셀·필드)은 모두 797곳이다. 이 중 세부내역서 `급여구분` `열추출`→`급여/비급여` 196곳은 관례 충돌로 따로 뺐다. 나머지 597곳은 sonnet 하위 에이전트 6개가 원본 이미지를 확대해 대조했다. 판정은 final(교정이 맞음)/ao(교정이 나빠짐)/neither(둘 다 틀림)/unclear로 나눴다.

## 테스트 중 고친 버그: `/api/verify`가 서버 전체를 막음

- 첫 배치에서 세부내역서 1건이 nginx 504(프록시 300초)로 실패했다. 서버 backend는 `unhealthy`였고 health 요청도 시간 초과가 났다.
- 원인은 `async def verify_result`가 수 분 걸리는 동기 함수 `verify.run`을 그대로 호출한 것이다. 요청 하나가 처리되는 동안 이벤트 루프 전체가 멈추고, 동시 요청은 줄을 서서 하나씩 처리된다. 하네스가 동시에 호출하면 같은 일이 생긴다.
- `await asyncio.to_thread(verify.run, ...)`로 바꿨다(`36da217`). 재배포 뒤 동시 4건을 처리하는 중에도 health가 0.01초에 200을 돌려준다.
- 긴 세부내역서(3~6분)는 nginx 300초 제한에 걸릴 수 있어서, 이번 배치는 backend 컨테이너로 직접 SSH 터널을 열어 보냈다. 운영에서 하네스가 frontend(nginx)를 거친다면 `proxy_read_timeout`을 늘려야 한다.

## 결과

### 진료비영수증 30건: 대체로 개선

| 판정 | 곳 |
|---|---|
| final(교정이 맞음) | **163** |
| ao(교정이 나빠짐) | 15 |
| neither | 10 |
| unclear | 2 |

- 룰 교정은 모두 원본과 맞았다.
  - `item_name`: `선별급여` 3건, `계`→`합계`, `보철-교정료`→`보철교정료`, 오독 교정
  - `column_shift`: 급여→비급여
  - `row_missing`: 금액이 빈 인쇄 행 채우기
- 이슈 문서들도 확인했다.
  - `202501020959270c`(비급여→급여 이슈)는 8곳 모두 맞게 교정됐다.
  - `SA2019123157847_…390b`(현상 1)는 9곳 모두 맞게 교정됐다. 공단부담총액 0→45,642로 하네스 `R-AC029-BURDEN` fail이 해소됐다.
  - `소계포함_…`(현상 3, 한방)는 공단부담총액 836,420 유지(합계 보조)와 환자부담총액 235,030→236,030이 맞았다. 반면 Docraft가 새로 넣은 행 이름 일부(`한약접약`, `65세이상등경감`, `보수처리조정금액`)와 `-6` 부호 누락은 틀렸다.
- 나빠진 곳은 네 종류다(감사 에이전트 하나가 집계 뒤 판정 파일 일부를 다시 확인해 덮어써서 최종 수치로 고쳤다).
  1. **합계 보조의 오선택(1건, 6곳):** `202501021624239c`에서 합계식 불일치가 2→0이 되는 Docraft 표를 택했다. 그런데 그 표는 합계 행 값을 `예약진찰료` 행에, `제증명/기타` 값을 `선별급여` 행에 넣은 것이었다. 같은 문서의 다른 6곳은 맞게 고쳤다. 합계만 맞고 행 배치가 틀린 표를 걸러내지 못했다.
  2. **금액산정 상자 행 밀림(2건):** Docraft 추출이 `납부할금액`·`이미납부한금액`·`납부한금액_현금`에 이웃 칸 값을 넣었고, Judge가 그걸 택했다.
  3. **금액산정 상자 빈 칸 채우기:** 인쇄상 빈 `납부할금액` 칸에 다른 합계로 계산한 값(예: 11,000)이 들어갔다.
4. **항목명:** `전혈및혈액성분제재료`(서식에 실제로 `제재료`로 인쇄됨)를 오독 교정이 `제제료`로 바꿨다. 이 밖에 Docraft 행 이름 오독(`치료재대`, `재활및물리치료`)도 있었다.

### 세부내역서 29건: 악화

| 판정 | 곳 |
|---|---|
| final | 84 |
| ao(교정이 나빠짐) | **303** |
| neither | 16 |
| unclear | 4 |
| 관례 충돌(`급여구분` `열추출`→`급여`/`비급여`) | 196(감사 제외) |

- **집계 행 삭제(221곳):** 인쇄된 소계·계·합계·끝수처리조정금액 행이 최종 결과에서 사라졌다.
  - 이번 AO 결과는 29건 중 25건에 집계 행 100개를 넣는다.
  - `rules.apply`는 집계 행을 진료비영수증 표에서만 남기고(`KEEP_TOTALS = {"진료비영수증"}`) 세부내역서 표에서는 지운다. 그래서 Docraft 표나 Judge가 고친 표(`_decide`가 `rules.apply`로 정규화)를 택할 때마다 집계 행이 빠진다.
  - 정답 라벨 세부내역서 303행에는 집계 행이 하나도 없어서 지금까지 평가에 드러나지 않았다.
- **같은 이름 행의 금액 뒤섞임:** `3022033115105207-1`은 날짜 블록마다 `이학요법료` 행이 셋이다. 같은 이름 행끼리 금액이 섞인 Docraft 표를 Judge가 택해 32곳 모두 AO보다 나빠졌다. `원내코드`가 이웃 행으로 밀린 문서(`SA2020010683384_…200`)도 있다.
- **그 밖:** 약품명 교정 오류(`코대원포르테시럽`→`코데핀포르테시럽`), `Level 8`→`Level B`, EDI코드 구분자 표기 등이 있었다.
- **맞게 고친 것(84곳):** `.0` 접미사 정리, EDI명칭 오탈자, 날짜, 중복 추출 행 제거 등이다.
- **`급여구분` `열추출`:** 이번 AO 결과에 339번 나온다. 급여구분이 인쇄되지 않은 서식에서 AO가 넣는 자리표시 값으로 보인다. Docraft·Judge는 이를 `급여`/`비급여`로 바꾼다. AO 관례를 확인해야 한다.

### 하네스 unresolved 필드

하네스가 unresolved로 남긴 필드는 57개다. Docraft가 값을 바꾼 것은 8개(영수증 6, 세부 2)이고, 나머지는 AO 값을 유지했다(agree 또는 ao). 바뀐 값 중 공단부담총액·환자부담총액은 합계식을 맞추는 방향으로 맞았다. 금액산정 상자의 `납부할금액`·`이미납부한금액`은 이웃 칸 값이라 틀렸다.

## 후속 수정과 세부내역서 재테스트

같은 29건을 수정 단계마다 AWS에서 다시 돌렸다. 변경 판정은 이전 감사와 같은 변경(행·열·AO값·최종값 동일)이면 재사용하고, 새로 생긴 변경만 다시 감사했다(2차 50곳, 최종 11곳).

| 수정 | 커밋 | 내용 |
|---|---|---|
| 집계 행 유지 | `d039862` | Docraft가 집계 행을 뽑지 않는 유형(`KEEP_TOTALS` 밖)은 AO의 소계·계·합계 행을 판정에서 빼 두었다가 최종 표의 원래 자리(AO에서 바로 앞 항목 행과 짝지어진 행 뒤)에 되돌린다(`verify._with_totals`). `_TOTAL_ROW`에 `총합계`를 더했다 |
| 금액 소수점 보존 | `2fc9fe5` | `_amount`가 숫자 아닌 글자를 모두 지워 `954.5`→`9545`, `19160.0`→`191600`이 됐다. 천 단위 구분은 빼고 소수 한두 자리는 살린다(`.0`은 정수, 소수 세 자리는 천 단위 구분). 합계식 금액(`_money`)은 원 단위로 버린다. 76건 rules 점수는 네 유형 모두 main과 같다 |
| 급여구분 교정 | `1ed825a` | 사용자 확인 결과 `열추출`은 AO 추출 오류다. 급여구분이 급여·비급여가 아니면 금액 열로 정한다. 본인·공단·전액본인부담에만 금액이 있으면 급여, 비급여에만 있으면 비급여이고, 정할 수 없으면 `item_class` 경고를 Judge에 넘긴다(`_class_checks`) |

| 세부내역서 29건 | 처음 | 집계 행 유지 후 | 최종(세 수정 모두) |
|---|---|---|---|
| AO 대비 변경(급여구분 제외) | 407 | 114 | 104 |
| final(교정이 맞음) | 84 | 81 | **78** |
| ao(교정이 나빠짐) | 303 | 16 | **9** |
| neither | 16 | 11 | 11 |
| unclear | 4 | 6 | 6 |

- 최종 결과는 다음과 같다.
  - AO 집계 행 100개가 모두 남았다.
  - 급여구분은 금액 열로 정해지는 190행이 모두 기대값과 일치하고, 어긋난 행은 0이다. 금액이 비어 정할 수 없는 30행은 `열추출` 그대로 `item_class` 경고로 남는다.
- `3022033115105207-1`(같은 이름 `이학요법료` 행이 여러 개, 소수 단가)은 변경이 32곳 → 9곳 → 3곳으로 줄었다.
- 남은 나빠짐 9곳과 둘 다 틀림 11곳은 흩어져 있다. 전액본인부담·비급여 열 혼동, 분수 기호(3/8→3/4), EDI코드 글자 오독, 인쇄되지 않은 값 채우기 등이다.

## 후속 과제 처리와 59건 최종 재테스트

| 과제 | 결과 |
|---|---|
| 합계 보조 조건 강화 | **보류.** 유일한 실패 사례(`202501021624239c`)는 합계 보조 문제가 아니라 아래 출력 셀 정렬 버그 때문에 생긴 착시였다. 합계 보조가 고른 Docraft 표는 맞았다 |
| 출력 셀 정렬(새로 발견) | `_table_rows`가 판정 행을 원래 AO 셀과 **자리 번호**로 이었다. 행이 끼워지면 뒤쪽 행이 다른 행(합계 등)의 셀을 물려받았고, `value`가 빈 칸은 `predicted_value`로 다른 행의 AO 값을 가진 것처럼 읽혔다. 이제 `pair_rows`로 짝이 되는 원래 행을 찾고, `_annotate`가 `predicted_value`를 최종값으로 맞춘다(AO 값은 `ao_value`). 커밋 `de2c0f0` |
| 같은 키 행 짝짓기 | `pair_rows`에서 키 열이 같은 후보가 여럿이면 나머지 칸이 더 많이 같은 행과 잇는다(`_likeness`, 동점이면 순서대로). 76건 rules 점수 변화 없음. 커밋 `9f51ed7` |
| 프록시 시간 제한 | `frontend/nginx.conf`와 Helm `frontend.yaml`의 `/api` `proxy_read_timeout`·`proxy_send_timeout`을 300s에서 900s로 늘렸다. 커밋 `4856b43` |

최종 재테스트는 AWS `618a93e` 배포에서 59건을 frontend(nginx) 경로로 보냈다. 58건이 성공했고 가장 긴 요청은 290초였다. 유형 코드가 잘못된 1건은 `doc_type`을 지정해 다시 보냈다. 이전 판정과 같은 변경은 재사용했고, 새 변경 106곳은 다시 감사했다.

| 유형 | final | ao(나빠짐) | neither | unclear |
|---|---|---|---|---|
| 진료비영수증 | **169** | 6 | 14 | 2 |
| 세부내역서(관례 차이 제외) | 81 | 13 | 13 | 15 |
| 세부내역서 원내코드 관례 차이 | 2 | 19 | – | – |

- 출력 확인
  - `202501021624239c`의 `예약진찰료`·`선별급여`가 비어 읽힌다.
  - 판정된 셀 중 `predicted_value`와 `value`가 다른 셀은 0이다.
  - 세부내역서 집계 행 100개가 모두 남았다.
  - 급여구분은 금액 열로 정해지는 192행이 일치한다. 1행은 `끝수처리 조정금액`을 Judge가 `공수처리 조정금액`으로 읽어 급여구분이 비었다.
- **원내코드 관례 차이(새 발견):** 이번 AO는 같은 코드를 `원내코드`·`EDI코드` 두 칸에 모두 넣는다(`AA156`/`AA156`). 정답 라벨 관례(`derive`)는 코드가 하나면 `EDI코드`에만 둔다. Judge가 표를 새로 쓴(`corrected`) 문서만 `rules.apply`를 거쳐 원내코드가 비워지므로, 같은 서비스 안에서 문서마다 결과가 달라진다. AO·하네스가 원하는 관례를 정해야 한다.
- 세부내역서 unclear 15곳은 대부분 여러 쪽 문서에서 요약 합계가 제공된 쪽에 없는 경우다.
- 남은 영수증 오류는 대부분 Docraft·Judge의 항목명 오독이다(`정맥수가원화외료`, `제층료` 등, neither).

## Docraft 추출 개선: 음수 부호·항목명 오독(2026-09-24)

| 개선 | 내용 | 확인 |
|---|---|---|
| 음수 부호 | 모델은 `-42,360`을 읽었는데 금액 정규화가 부호를 지웠다. `_amount`가 앞의 `-`·`−`·`△`를 살리고, `_money`는 정규화 값을 그대로 정수로 바꾼다 | 파싱 에러 이슈 문서에서 Docraft 값도 `-42360`이 됐다 |
| 항목명 오독 | `_misread`가 네 글자 이름도 보고, 이름 중간에서 한 글자가 빠지거나 더해진 경우도 표준 이름 하나와만 가까우면 고친다(`한약접약`→`한약첩약`, `치료재대`→`치료재료대`). 표준 이름에 `끝수처리조정금액`·`정액수가완화의료`·`질병군포괄수가`를 더했다(`보수처리조정금액`→`끝수처리조정금액`) | 단위 테스트 |

- 끝에 글자가 덧붙은 변형(`재활및물리치료`/`재활및물리치료료`)은 교정하지 않는다. 처음에는 교정했는데 라벨 1건이 바뀌었고, 원본에 실제로 `재활 및 물리치료`로 인쇄돼 있었다. 서식마다 끝의 `료`를 붙이거나 빼는 인쇄 변형이다.
- 두 글자 이상 다른 오독(`65세이상등경감`, `정맥수가원화외료`)은 멀쩡한 이름까지 바꿀 위험이 커서 다루지 않는다.
- 76건 rules: 영수증 4261→4262, 나머지 유형은 그대로. 라벨 511행 중 교정으로 바뀌는 이름은 0이다.
- 금액산정 상자 칸 밀림은 서식마다 계산식이 달라 룰로 판정하기 어려워 보류했다.
- 한방 문서 재실행에서 Docraft 추출이 한방 선택항목 행을 아예 뽑지 않아 이름 교정을 실제 문서로 확인하지 못했다. 또 누락 행 보충(`RECEIPT.ROW_MISSING`)이 Docraft의 두 글자 오독 이름 `치료제료다`를 AO 표에 끼워 `치료재료대`와 중복 행이 생겼다. 이름이 기존 행과 비슷하면 끼우지 않는 조건이 필요하다.

## 후속 과제

1. ~~세부내역서 집계 행 유지~~: 완료(위 표).
2. ~~`열추출` 관례 확인~~: AO 추출 오류로 확인했고, 금액 열로 교정하도록 했다.
3. ~~합계 보조 조건 강화~~: 보류(실패 사례가 출력 셀 정렬 버그였다).
4. ~~같은 이름 행 짝짓기~~: 완료.
5. ~~nginx `proxy_read_timeout`~~: 900초로 완료.
6. ~~세부내역서 원내코드 관례~~: 사용자 결정에 따라 AO처럼 두 칸 모두 유지하기로 했다(`b4c246a`).
   - 이번 AO는 코드 열이 하나인 서식에서는 코드를 `EDI코드`에만 둔다(219행). 코드 열이 둘인 서식에서는 같은 코드를 두 칸 모두에 둔다(30행).
   - `derive`에서 "원내코드 = EDI코드면 원내코드를 비움"을 없앴다. "원내코드만 있으면 EDI코드로 옮김"은 유지한다.
   - 이것만 바꾸면 Docraft 추출 모델이 코드 열 하나인 서식에서도 같은 코드를 두 칸에 적어, 76건 세부내역서 fp가 89→162로 늘었다. 그래서 `_header_columns`에서 파싱 머리글의 코드 열이 둘 미만이면 같은 코드의 원내코드를 비운다. 파싱 블록이 없는 Judge 교정 표에는 적용하지 않아 AO 값이 유지된다. 이후 fp는 다시 89다.
   - 재배포(`2b13399`) 후 5건을 다시 돌렸다. 두 칸 같은 코드가 AO와 같게 유지됐다(7/7, 9/9, 3/3, 1/1).
   - 2026-09-24 사용자 결정으로 원내코드만 있는 행도 AO처럼 원내코드에 둔다(`a6a7530`). `derive`의 "원내코드만 있으면 EDI코드로 옮김"을 없앴다.
     - 대신 `_header_columns`에서 머리글 코드 열이 하나인 Docraft 추출만 EDI코드로 모은다.
     - 명칭이 코드 칸으로 밀린 열 밀림 교정(원내코드 자리 코드를 EDI코드로)은 유지했다.
     - 76건 세부내역서 rules는 3940/4189, fp 89→88이다. 배포는 `9aa5647`이다(다른 세션의 요청 처리 중이라 끝난 뒤 배포).
